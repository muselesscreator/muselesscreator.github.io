export type ProjectData = {
  title: string;
  description: string;
  category: string;
  githubUrl: string;
  githubRepo: string;
  projectUrl: string;
  packageName?: string;
  packageUrl?: string;
  screenshots?: string[];
  details?: string;
  image?: string;
};
